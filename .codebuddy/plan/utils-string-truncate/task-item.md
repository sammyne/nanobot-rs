# 实施计划

- [ ] 1. 创建 utils crate 基础结构
   - 在 `crates/utils` 目录下创建完整的 crate 目录结构
   - 创建 `Cargo.toml` 文件，配置库类型和基本信息
   - 创建 `src/lib.rs` 和 `src/strings.rs` 文件
   - 在根工作空间的 `Cargo.toml` 中添加 `utils` crate 作为成员
   - _需求：1.1、1.2、1.3、1.4、1.5_

- [ ] 2. 实现 truncate 函数
   - 在 `strings.rs` 中编写 `truncate` 函数，接受字符串引用和最大字符数参数，返回 &str
   - 使用 `char_indices()` 遍历字符串，确保在合法的 UTF-8 字符边界上截取
   - 处理空字符串和短字符串的边界情况
   - _需求：2.1、2.2、2.3、2.4、2.5_

- [ ] 3. 为 truncate 函数添加单元测试
   - 在 `strings.rs` 中编写表驱动测试用例
   - 覆盖空字符串、短字符串、长字符串的测试场景
   - 添加中文字符等多字节字符的测试用例
   - 测试截断位置正好在字符边界的边界情况
   - 验证截断标记正确添加
   - _需求：5.1、5.2、5.3、5.4、5.5、5.6、5.7_

- [ ] 4. 添加函数文档和代码质量保证
   - 为 `truncate` 函数添加详细的文档注释，说明安全性、UTF-8 处理方式和使用示例
   - 运行 `cargo clippy` 检查并修复所有警告
   - 运行 `cargo fmt` 确保代码符合项目 Rust 代码风格规范
   - _需求：6.1、6.2、6.3、6.4、6.5_

- [ ] 5. 在 session crate 中应用 truncate 方法
   - 在 session crate 的 `Cargo.toml` 中添加 `utils` 依赖
   - 在 `session.rs` 中引入 `utils::strings::truncate`
   - 替换现有的手动截取逻辑为 `truncate` 函数调用
   - 移除不再需要的手动字符边界遍历代码
   - 保持 `TOOL_RESULT_MAX_CHARS` 常量定义不变
   - _需求：3.1、3.2、3.3、3.4_

- [ ] 6. 搜索项目中其他使用字符串下标截取的位置
   - 在整个项目中搜索使用 `&s[..n]` 形式的字符串截取代码
   - 识别所有潜在的 UTF-8 边界问题位置
   - 评估每个位置是否需要使用安全的 `truncate` 方法
   - _需求：4.1、4.2_

- [ ] 7. 替换其他 crate 中的不安全字符串截取
   - 为需要替换的 crate 在 `Cargo.toml` 中添加 `utils` 依赖
   - 使用 `truncate` 方法替换不安全的字符串下标截取
   - 验证替换后的行为与原有实现一致
   - _需求：4.3、4.4、4.5_
