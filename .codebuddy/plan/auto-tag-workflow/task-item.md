# 实施计划

- [ ] 1. 创建自动打 tag 工作流文件结构
   - 创建 `.github/workflows/auto-tag.yml` 文件
   - 配置工作流名称、触发条件（push 到 main 分支）
   - _需求：4.1、4.2_

- [ ] 2. 实现提交消息检测逻辑
   - 添加步骤提取最新提交消息
   - 使用正则表达式检测 `bump v{version}` 模式
   - 配置条件判断，仅在匹配时执行后续步骤
   - _需求：1.1、1.2、1.3_

- [ ] 3. 实现版本号解析与验证
   - 编写正则表达式提取版本号（支持语义化版本格式）
   - 添加步骤验证版本号格式有效性
   - 添加步骤检查 tag 是否已存在于远程仓库
   - _需求：2.1、2.2、2.3_

- [ ] 4. 实现 tag 创建与推送
   - 配置 Git 用户信息（使用 GitHub Actions bot）
   - 创建轻量级 tag 并关联到当前 commit
   - 推送 tag 到远程仓库，配置重试机制
   - _需求：3.1、3.2、3.3_

- [ ] 5. 完善错误处理与日志输出
   - 在关键步骤添加详细的日志输出（echo 语句）
   - 添加错误处理逻辑，输出明确的错误信息
   - 配置权限不足时的提示信息
   - _需求：5.1、5.2、5.3_

- [ ] 6. 配置工作流权限
   - 设置 `contents: write` 权限以允许创建和推送 tag
   - 验证 GITHUB_TOKEN 的权限配置
   - _需求：3.3、5.3_

- [ ] 7. 测试与验证工作流
   - 创建测试提交，提交消息包含 `bump v0.0.1`
   - 验证工作流是否正确触发并创建 tag
   - 验证 tag 是否成功推送到远程仓库
   - _需求：全部验收标准_
