# 实施计划

- [ ] 1. 添加 config crate 依赖
   - 在工作空间根 `Cargo.toml` 中添加 `config` crate 作为工作空间依赖
   - 在 `crates/config/Cargo.toml` 中引用该依赖
   - _需求：1.1_

- [ ] 2. 重构配置加载逻辑
   - 修改 `crates/config/src/lib.rs` 中的 `Config::load()` 方法，使用 `config` 库加载配置文件
   - 保持现有的 API 接口不变，确保向后兼容
   - 保留配置文件路径 `~/.nanobot/config.json` 的支持
   - _需求：1.2、1.3、1.4_

- [ ] 3. 实现环境变量覆盖机制
   - 配置 `config` 库的环境变量源，使用 `NANOBOT_` 前缀和双下划线分隔符
   - 实现环境变量到配置结构的自动映射
   - 确保环境变量优先级高于配置文件
   - _需求：2.1、2.2、2.3、2.5_

- [ ] 4. 增强错误处理
   - 扩展 `ConfigError` 枚举，添加环境变量解析错误类型
   - 实现类型不匹配时的清晰错误提示
   - 保持与现有错误处理风格一致
   - _需求：4.1、4.2、4.3、4.4_

- [ ] 5. 编写单元测试
   - 测试配置文件正常加载
   - 测试环境变量覆盖字符串、数字、布尔值等类型
   - 测试环境变量优先级高于配置文件
   - 测试边界情况（空值、特殊字符、类型不匹配）
   - _需求：5.1、5.2、5.3、5.4_

- [ ] 6. 更新项目文档
   - 在代码注释中添加环境变量映射说明
   - 更新 README 文档，添加环境变量配置章节
   - 提供常用环境变量示例
   - _需求：3.1、3.2、3.3、3.4_
